{
    "name": "@xch/class-names",
    "version": "0.1.1",
    "description": "Utility function for building className strings",
    "main": "lib/index.js",
    "types": "lib/index.d.ts",
    "engines": {
        "node": ">=10"
    },
    "scripts": {
        "start": "npm run build:dev-watch",
        "build": "tsc",
        "build:dev-watch": "tsc --watch",
        "clean:build": "rm -rf lib",
        "prepare": "npm run clean:build && npm run build",
        "test:dev-watch": "jest --watch",
        "test": "jest"
    },
    "author": "Xingchen Hong",
    "license": "MIT",
    "dependencies": {},
    "devDependencies": {
        "@types/jest": "^25.1.0",
        "@types/node": "^13.5.1",
        "jest": "^25.1.0",
        "ts-jest": "^25.0.0",
        "ts-node": "^8.6.2",
        "typescript": "^3.7.5"
    }
}
